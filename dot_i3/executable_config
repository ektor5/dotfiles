# Thise soddisfa i requisiti.file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout somewhen, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

# font for window titles. ISO 10646 = Unicode
#font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
set $ft pango:Terminus 12
font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

#..........
# Look
#..........

new_window pixel 2
new_float pixel 2

# Hybrid colors
set $back               #1d1F21
set $black              #282A2E
set $grey               #373B41
set $lightgrey          #707880
set $white              #C5C8C6
set $yellow             #F0C674
set $red                #CC6666
set $darkred            #A54242
set $green              #B5BD56

#class                  border      backgr.     text        split
#client.focused          $darkred     $darkred       $black      $red
#client.focused_inactive $grey       $grey       $lightgrey  $grey
#client.unfocused        $grey       $grey       $lightgrey  $grey
#client.urgent           $red        $red        $black      $red
#client.background       $back


#### WORKSPACES

set $w1 1:🌍
set $w2 2:📁
set $w3 3:🖧
set $w4 4:
set $w5 5:🎮
set $w6 6:⣶
set $w7 7:⣾
set $w8 8:⣿
set $w9 9:9
set $w10 10:

# switch to workspace
# browser
bindsym $mod+1 workspace $w1
# coding
bindsym $mod+2 workspace $w2
bindsym $mod+3 workspace $w3
# music/sound
bindsym $mod+4 workspace $w4
bindsym $mod+5 workspace $w5
bindsym $mod+6 workspace $w6
bindsym $mod+7 workspace $w7
bindsym $mod+8 workspace $w8
bindsym $mod+9 workspace $w9
# messenger
bindsym $mod+0 workspace $w10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace $w1
bindsym $mod+Shift+2 move container to workspace $w2
bindsym $mod+Shift+3 move container to workspace $w3
bindsym $mod+Shift+4 move container to workspace $w4
bindsym $mod+Shift+5 move container to workspace $w5
bindsym $mod+Shift+6 move container to workspace $w6
bindsym $mod+Shift+7 move container to workspace $w7
bindsym $mod+Shift+8 move container to workspace $w8
bindsym $mod+Shift+9 move container to workspace $w9
bindsym $mod+Shift+0 move container to workspace $w10

workspace_auto_back_and_forth no
bindsym $mod+BackSpace workspace back_and_forth

bindsym $mod+equal workspace next_on_output
bindsym $mod+minus workspace prev_on_output

for_window [window_role="task_dialog|bubble|pop-up|page-info|Preferences"] floating enable

### EXEC

# autostart
exec virt-manager 
assign [class="Virt-manager"] $w8
#for_window [class="Virt-manager"] move container to workspace 6

exec ~/telegram/Telegram/Telegram
assign [class="Telegram"] $w10

exec firefox

exec blueman-tray
exec mate-power-manager

# start a terminal
bindsym $mod+Return exec i3-sensible-terminal

# screenlock
bindsym $mod+Shift+Ctrl+L exec xautolock -locknow

# start Chromium
bindsym $mod+Shift+b exec firefox 
bindsym $mod+b [class="firefox"] focus

# start weechat
bindsym $mod+i [title="WeeChat*"] focus
bindsym $mod+Shift+i exec i3-sensible-terminal -e weechat

# start arandr
bindsym $mod+Shift+u exec arandr 

# start gvim
bindsym $mod+Shift+v exec gvim
bindsym $mod+Shift+c exec gvim ~/.i3/config

# start musixmatch
bindsym $mod+Shift+t exec musixmatch

# screenshot
bindsym --release Print exec scrot -s -e 'mv $f ~/scrot/'

#for_window [instance="arandr"] move to workspace 9 ## annoying

# start dmenu (a program launcher)
bindsym $mod+p exec dmenu_run -nb "$back" -nf "$white" -sb "$grey" -sf "$darkred"

#start music player 
bindsym $mod+Ctrl+a exec i3-sensible-terminal -e ncmpcpp
bindsym $mod+Shift+a exec spotify

# Media player controls
bindsym Ctrl+XF86AudioPlay exec mpc toggle
bindsym XF86AudioPlay exec playerctl play-pause
bindsym XF86AudioNext exec playerctl next
bindsym XF86AudioPrev exec playerctl previous

bindsym $mod+Shift+period exec playerctl next
bindsym $mod+Shift+comma exec playerctl previous

# Pulse Audio controls
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume \
  alsa_output.pci-0000_00_1b.0.analog-stereo +5% #increase sound volume 
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume \
  alsa_output.pci-0000_00_1b.0.analog-stereo -5% #decrease sound volume
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute \
  alsa_output.pci-0000_00_1b.0.analog-stereo toggle # mute sound

# Screen brightness controls

 # increase screen brightness
bindsym XF86MonBrightnessUp exec xbacklight -inc 5 
 # decrease screen brightness
bindsym XF86MonBrightnessDown exec xbacklight -dec 5

#start file manager
bindsym $mod+Shift+f exec i3-sensible-terminal -e ranger

#start xkill
bindsym $mod+Shift+x exec xkill

#start audio mixer
bindsym $mod+Shift+p exec pavucontrol

#start editor
bindsym $mod+Shift+n exec kate
bindsym $mod+n [class="kate"] focus

# reboot system
bindcode $mod+Ctrl+119 exec systemctl reboot


### FOCUS

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

bindsym $mod+Tab focus left

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# focus the parent container
bindsym $mod+a focus parent
# focus the child container
bindsym $mod+d focus child

# focus to urgent window
bindsym $mod+x [urgent=latest] focus


#### MOVE

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# alternatively, you can use the cursor keys for moving between outputs:
bindsym $mod+Shift+Ctrl+Left move workspace to output left
bindsym $mod+Shift+Ctrl+Down move workspace to output down
bindsym $mod+Shift+Ctrl+Up move workspace to output up
bindsym $mod+Shift+Ctrl+Right move workspace to output right


#### WIN MODES

# split in horizontal orientation
bindsym $mod+v split h

# split in vertical orientation
bindsym $mod+s split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen

# change container layout (stacked, tabbed, default)
bindsym $mod+w layout stacking
bindsym $mod+t layout tabbed
bindsym $mod+e layout default


#### TILING 

# toggle tiling / floating
bindsym $mod+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+Shift+space focus mode_toggle

# reload the configuration file
bindsym $mod+Ctrl+R reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+R restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+E exit

# kill focused window
bindsym $mod+c kill


#### Misc
#bindsym $mod+Shift+t exec i3-sensible-terminal -e \
	#'bash -c "echo Thanks for contacting UDOO Customer Care | xsel -b"'


#### MODES
set $screen screen: 1 [norm] 2 [work] 3 [work-e] 4 [film] 5 [bunk] 6 [work-se]
mode "$screen" {
        bindsym 1 exec i3-sensible-terminal -e ~/.screenlayout/normal.sh
        bindsym 2 exec i3-sensible-terminal -e ~/.screenlayout/work.sh
        bindsym 3 exec i3-sensible-terminal -e ~/.screenlayout/work-easy.sh
        bindsym 4 exec i3-sensible-terminal -e ~/.screenlayout/film.sh
        bindsym 5 exec i3-sensible-terminal -e ~/.screenlayout/bunker.sh
        bindsym 6 exec i3-sensible-terminal -e ~/.screenlayout/work-se.sh

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}
bindsym $mod+Shift+s mode "$screen"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym h resize shrink width 10 px or 10 ppt
        bindsym j resize grow height 10 px or 10 ppt
        bindsym k resize shrink height 10 px or 10 ppt
        bindsym l resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindcode 113 resize shrink width 10 px or 10 ppt
        bindcode 116 resize grow height 10 px or 10 ppt
        bindcode 111 resize shrink height 10 px or 10 ppt
        bindcode 114 resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}
bindsym $mod+r mode "resize"

#mount/unmount partitions
set $mount mount: 0: /dev/sdd 1: /dev/sdd1 2: /dev/sdd2 3: /dev/sde1 4: /dev/sde2 5: /dev/mmcblk0p1 6: /dev/mmcblk0p2
mode "$mount" { 
    bindsym 0 exec i3-sensible-terminal -e 'udisksctl mount -b /dev/sdd'
    bindsym 1 exec i3-sensible-terminal -e 'udisksctl mount -b /dev/sdd1'
    bindsym 2 exec i3-sensible-terminal -e 'udisksctl mount -b /dev/sdd2'
    bindsym 3 exec i3-sensible-terminal -e 'udisksctl mount -b /dev/sde1'
    bindsym 4 exec i3-sensible-terminal -e 'udisksctl mount -b /dev/sde2'
    bindsym 5 exec i3-sensible-terminal -e 'udisksctl mount -b /dev/mmcblk0p1'
    bindsym 6 exec i3-sensible-terminal -e 'udisksctl mount -b /dev/mmcblk0p2 '
    bindsym Return mode "default"
    bindsym Escape mode "default"
}
bindsym $mod+m mode "$mount"

set $unmount unmount: 0: /dev/sdd 1: /dev/sdd1 2: /dev/sdd2 3: /dev/sde1 4: /dev/sde2 5: /dev/mmcblk0p1 6: /dev/mmcblk0p2
mode "$unmount" { 
    bindsym 0 exec i3-sensible-terminal -e 'udisksctl unmount -b /dev/sdd'
    bindsym 1 exec i3-sensible-terminal -e 'udisksctl unmount -b /dev/sdd1'
    bindsym 2 exec i3-sensible-terminal -e 'udisksctl unmount -b /dev/sdd2'
    bindsym 3 exec i3-sensible-terminal -e 'udisksctl unmount -b /dev/sde1'
    bindsym 4 exec i3-sensible-terminal -e 'udisksctl unmount -b /dev/sde2'
    bindsym 5 exec i3-sensible-terminal -e 'udisksctl unmount -b /dev/mmcblk0p1'
    bindsym 6 exec i3-sensible-terminal -e 'udisksctl unmount -b /dev/mmcblk0p2 '
    bindsym Return mode "default"
    bindsym Escape mode "default"
}
bindsym $mod+Shift+m mode "$unmount"

#format things in clipboard
set $format format clipboard: 1: justify 2: column 3: sort 4: wc 5: date
mode "$format" { 
	bindsym 1 exec i3-sensible-terminal -e \
		'bash -c "xsel -b | par | sponge | xsel -b"'
	bindsym 2 exec i3-sensible-terminal -e \
		'bash -c "xsel -b | column -t | sponge | xsel -b"'
	bindsym 3 exec i3-sensible-terminal -e \
		'bash -c "xsel -b | sort | sponge | xsel -b"'
	bindsym 4 exec i3-sensible-terminal -e \
		'bash -c "xsel -b | wc && read"'
	bindsym 5 exec i3-sensible-terminal -e \
		'bash -c "date +%Y%m%d | xsel -b"'
	bindsym Return mode "default"
	bindsym Escape mode "default"
}
bindsym $mod+Shift+w mode "$format"

bindsym $mod+Shift+d exec i3-sensible-terminal -e \
		'bash -c "date +%Y%m%d | xsel -b"'

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
status_command i3status
        status_command i3blocks
        font pango:Terminus, Icons 10
        tray_output primary
        strip_workspace_numbers yes
}
